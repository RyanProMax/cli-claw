/**
 * Thin type re-export wrapper around the canonical shared StreamEvent build output.
 * Source of truth: shared/stream-event.ts
 */

export type {
  StreamEvent,
  StreamEventType,
  StreamRuntimeIdentity,
} from '../../../shared/dist/stream-event.js';
